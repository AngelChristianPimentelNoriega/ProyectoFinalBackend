server:
  port : 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AngelChristianPimentelNoriega/config-data
          search-paths: api
          default-label: "master"
  security:
    user: 
      name: root
      password: password